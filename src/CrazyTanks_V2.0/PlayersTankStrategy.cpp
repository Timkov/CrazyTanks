#include "PlayersTankStrategy.h"

typeh::Direction PlayersTankStrategy::chooseDirection(const Unit& player, const Unit& gold, 
                                                                          const int x, const int y) const {
  return typeh::DOWN;
}